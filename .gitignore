.venv/
__pycache__/
.ipynb_checkpoints/
data/cache/
# Committed odds bundle (data/odds/) is intentionally NOT ignored — it ships
# with the repo so Streamlit Cloud can run backtests without a local cache.
# Regenerate with: python scripts/build_odds_bundle.py

.hooplytics_cache/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store

# Claude Code local state
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Secrets — never commit
.env
.env.*
!.env.example
.streamlit/secrets.toml
.streamlit/secrets.toml.*
*.secret
secrets.*
